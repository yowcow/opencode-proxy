{
  "name": "opencode-proxy",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "wrangler": "^3.114.17"
  },
  "description": "The official AnythingLLM Android app cannot send the custom `x-opencode-session` header, so connecting directly to opencode Zen/Go (`https://opencode.ai/zen/go/v1`) fails with `400 MissingSessionID`. This Worker is a minimal proxy that auto-attaches that header.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "type": "commonjs"
}
